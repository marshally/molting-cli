# SQLite databases
*.db
*.db?*
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock

# Legacy database files
db.sqlite
bd.db

# Merge artifacts (temporary files from 3-way merge)
beads.base.jsonl
beads.base.meta.json
beads.left.jsonl
beads.left.meta.json
beads.right.jsonl
beads.right.meta.json

# When using beads-metadata branch, issues.jsonl should only be tracked there
# Keep metadata and config files in main branch
!metadata.json
!config.json

# Issue data - tracked only on beads-metadata branch (protected branch mode)
issues.jsonl
beads.jsonl
